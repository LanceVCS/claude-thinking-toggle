# Claude Thinking Visibility Patch

Makes Claude Code's thinking blocks visible by default, without needing to toggle via `ctrl+o`.

## Usage

```bash
node thinker.js           # Apply patch
node thinker.js --restore # Restore original
```

Requires Node.js and Claude Code. Re-run after Claude Code updates.
